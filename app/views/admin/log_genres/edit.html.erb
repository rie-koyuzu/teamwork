<div class="row">
    <div class="col-xs-10 col-xs-offset-1">
      <h2>ログジャンル編集</h2><br>

    <div class="edit-genre-form">
    <%= form_with model: @log_genre, url: admin_log_genre_path do |f| %>
          <%= f.label :ジャンル %>
          <%= f.text_field :name,class: "col-md-4" %>
          <%= f.submit "変更を保存する" ,class: "btn btn-primary" %>
        </div>
    <% end %>
  </div>
</div>
